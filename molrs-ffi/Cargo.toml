[package]
name = "molrs-ffi"
version = "0.1.0"
edition = "2024"
authors = ["MolCrafts"]
description = "FFI layer for molrs with handle-based abstractions"
license = "BSD-3-Clause"

[dependencies]
molrs = { path = "../molrs-core", package = "molrs-core", default-features = false }
slotmap = "1.0"

[dev-dependencies]
ndarray = "0.17"
