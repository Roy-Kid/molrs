//! Mirrors `src/smiles/`. SMILES/SMARTS parsing is a string parser, so these
//! are public-API integration tests over a literal corpus rather than
//! `tests-data/` files.

#[path = "smarts.rs"]
mod smarts;
