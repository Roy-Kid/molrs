# molrs — Spec Index

One row per spec produced by `/mol:spec`. Newest on top.

| 2026-06-18 | [opls-typifier-01-typing](opls-typifier-01-typing.md) | approved | molrs-ff | OPLS 链 1/3：SMARTS 原子分型下沉（**反转 [opls-ef-01](opls-ef-01-kernels-seam.md) 的"typifier 不下沉 B线"**）；新增 `OplsTypingMeta`(class/def/overrides/priority/layer，镜像 MMFFParams) + `read_opls_typing_xml_str`（`read_opls_xml` 势能解析不动）+ `ff/typifier/opls` 用原生 SMARTS `find_matches` + 优先级/overrides/layer 排序 → typed Atomistic；逐位复刻 molpy `_OplsAtomTypifier`；legacy 无-def 行 out of scope |
| 2026-06-18 | [opls-typifier-02-assign](opls-typifier-02-assign.md) | approved | molrs-ff | OPLS 链 2/3：成键参数匹配下沉，特异性排序匹配器（`_end_score` 3/1/0/None + `_sequence_score` 双向 + `(score,layer)` 排序，复刻 molpy `ForceField*Typifier`；`get_bondtype` 精确语义不改）；no-match 接缝交 [ff-parameter-estimator](ff-parameter-estimator.md)（与 gaff-03 同语义）；`build()` 闭环 typify→assign→to_frame→to_potentials；依赖 01 |
| 2026-06-18 | [opls-typifier-04-layered-deps](opls-typifier-04-layered-deps.md) | approved | molrs-ff (+core/chem/smarts) | OPLS 分型扩展（执行序在 02 后、03-parity 前）：补齐 chain-1 跳过的 **133 个 `%opls_NNN` 依赖型 def**——通用 context-label SMARTS 扩展(`%LABEL`→`HasContextLabel`+查赋型表谓词，旧入口不变) + `OplsDependencyAnalyzer`(Kahn 分层+Tarjan SCC) + `LayeredTypingEngine`(逐层+环组定点迭代)，复刻 molpy graph/dependency_analyzer/layered_engine；annotate_opls 退化为 level-0；chain-3 全量 parity 硬前置 |
| 2026-06-18 | [opls-typifier-03-parity](opls-typifier-03-parity.md) | approved | molrs-ff | OPLS 链 3/3：对照 molpy `OplsTypifier` 验证台（gen_opls_fixtures.py 产 per-atom type+params JSON→tests-data/opls/），逐原子 type 100% 一致 + 成键参数容差内（r0 0.02Å/θ0 3°/k rtol 0.10）`type:scientific` 门，缺 fixtures 干净跳过；删 molpy Python 分型的硬门；依赖 01+02 |
| 2026-06-18 | [ff-parameter-estimator](ff-parameter-estimator.md) | approved | molrs-ff | FF 无关相似性缺参估计器，挂 OPLS [opls-typifier-02](opls-typifier-02-assign.md) + GAFF [gaff-typifier-03](gaff-typifier-03-assign.md) 的 no-match：parmchk2 类比级联 + CGenFF 加性惩罚(内原子×10) + GAFF 经验兜底(Badger 键k/角θ0均值/Eq.5 Kθ/二面角通用回退或近零势垒**绝不伪造**)；最近类比直接复制不平均；溯源{analog,penalty,method}+分级<10/10–50/>50；opt-in、strict 不介入；取代已作废的 molpy-Python 版 |
| 2026-06-18 | [compute-fit-01-module](compute-fit-01-module.md) | approved | molrs (compute) | **计算↔拟合分离 链 1/3（非破坏新增）**。新增与 `Compute` 并列的 `Fit` trait（消费上游曲线/ACF，非帧）+ `compute::fit` 子模块：数值拟合 `LinearFit`(OLS 抽自 dielectric)/`RunningIntegral`(梯形抽自 jacf)/`Plateau`/`DebyeFit`(收口 molpy ad-hoc)，谱变换 `PowerSpectrum`/`IRSpectrum`/`RamanSpectrum`(组合 `molrs::signal` 窗，不重写)；新增只返回原始曲线的 method-first OOP compute `VACF`/`EinsteinDiffusion`/`GreenKuboDiffusion`/`EinsteinConductivity`/`GreenKuboConductivity`/`DebyeRelaxation`。三不变量：(a)max_lag≥下游消费 (b)ACF 非归一化(Debye 幅度来自⟨M²⟩) (c)V/T/Ewald-BC/电流归一化随结果携带。谱 Fit 逐位复现旧 power/ir/raman；旧自由函数本阶段全保留。science verified（PRL122/PCCP15/JCP159 + Frenkel&Smit/Hansen&McDonald）。NernstEinstein 是「派生之派生」消费 Dᵢ 标量。refines `vibrational-spectra` 谱输出契约（仅说明不改）。/mol:map 蓝图刷新延后 |
| 2026-06-18 | [compute-fit-02-repoint](compute-fit-02-repoint.md) | approved | molrs-python, molpy, bm-molrs-molpy | **计算↔拟合分离 链 2/3（仍非破坏）**。把 01 的裸 compute+fit 经 PyO3 暴露（新 `#[pyclass]` 注册到 `molrs` 顶层）；molpy compute 包装薄继承/委托到 molrs（core 下沉，molpy 不留拟合/谱数学，ad-hoc DebyeFit 改委托）；freud-parity benches 改 raw→fit 调用、**保留双预算 floor 不收紧**。旧自由函数绑定保留+加 `DeprecationWarning`、返回形状不变。核心验收=数值回归锁（σ/D/谱在容差内复现旧路径）。前置：必须重建 maturin wheel（molpy 测试跑已装 wheel）。bm-molrs-molpy 为 sibling 仓、本 checkout 无，路径描述性引用 |
| 2026-06-18 | [compute-fit-03-cleanup](compute-fit-03-cleanup.md) | approved | molrs (compute, BREAKING), molrs-python, molpy, benches | **计算↔拟合分离 链 3/3（BREAKING）**。删 `ConductivityResult` 的 sigma/slope/fit_start/fit_end、`JacfResult` 的 sigma/sigma_running（或整删 bundled 类型）；残余自由函数 `onsager_correlation`/`power_spectrum`/`ir_spectrum`/`raman_spectrum`/`einstein_helfand_*`/`green_kubo_*` 转 OOP `Compute` 或删；删死代码 `window_and_fft`/`windowed_acf_spectrum`/`acf_to_spectrum`/`acf_to_intensities` 及内联 OLS/梯形；删弃用 PyO3 绑定 + molpy 过渡 shim；spectra 只返回 raw 未加窗 ACF。验收含 grep-clean(全仓零引用被删符号) + 物理逐值不变回归 + 全套 cargo/clippy/fmt 绿。记录 breaking SemVer 跳版（不发布；molpack/molpy exact-pin 待更）。不动 gaussian_density/diffraction smear/local_density taper/finalize 归一化 |
| 2026-06-14 | [ff-perinstance-params](ff-perinstance-params.md) | draft | molrs-ff (potential / typifier/mmff / mmff/energy→relocate), molrs-conformer (ETKDG), molrs-python | **把「参数赋值」和「能量求值」彻底分开（通用、面向更多力场）**。替换已退役的 ff-mmff-unify-generic-path 的 kernel/参数机制:**标签→共享表** 装不下 MMFF 的等价回退 + 经验规则(benzene 卡 `mmff_stbn: unknown '1_37_37_37'`、ethylene 差 8.1),故其 ac-004 不可达。**新模型**:力场 = 参数分配器,产出「每个键/角/二面角都带数值参数」的结构;kernel = 纯求值器,**读逐实例参数列**(如 mmff_stbn 读 r0_ij),不查共享表、不含力场专属解析。MMFF 落地 = **复用** RDKit-validated 的 `MmffForceField` 解析(`params.rs` 从 `energy/` 搬出保留),typifier 逐项算数值挂 Atomistic→to_frame 成列→`mmff_*` kernel 读列;数值零重推。门禁:全 fixtures(含芳香/sp2)RDKit parity(ac-003)绿后才删单体(ac-006)。现状全绿:typify 复用前端 + OOP 枚举/`resolve_*_label` + 空块跳过已做,**e_ethane parity 2.3e-5**。表力场(GAFF/OPLS)不动。吸收已退役 ff-mmff-unify 的 ac-005/006/007 |
| 2026-06-13 | [interop-00-design](interop-00-design.md) | design | molpy/molrs/downstream (umbrella) | **顶层设计,先 review 再实施**。三方联动 + 用户侧 API 长相,统领下面三份实现 spec。三方:molpy(纯 Python,Frame 即 molrs.Frame)/ molrs(数据类型+capsule 传输+io.forcefield+to_potentials+kernels)/ 下游 Rust 应用(链 `molcrafts-molrs`+`molrs-ffi`,无自有数据层)。**数据归属**:ForceField=参数+special_bonds 权重(非 nblist);nblist 归消费方;没人 marshal。**Python API**:`ff=molrs.io.forcefield.read_lammps(path)` → `relaxer=molpack.LBFGSRelaxer(ff, chain)` → `Target.with_relaxer` → `result.frame` 是真 molrs.Frame(全程无 to_dict/from_dict/from_lammps_ff/mpk.Frame)。**Rust API**:`use molrs_ffi::prelude::*; obj.as_frame_ref()?/as_forcefield_ref()?; frame_ref.into_py(py)`。列了 6 个**待定 API 决策**(relaxer 构造形态、bridge trait vs free fn、reader 入口、molpy frame 保证、返回对象、命名)要先拍。定了再按 interop-ffi-bridge→ff-special-bonds-nblist→interop-wrapup 实施 |
| 2026-06-13 | [interop-wrapup](interop-wrapup.md) | draft | molrs-ffi, molrs-python, molrs-ff, molrs-capi, docs (consumer: molpack) | 两份实现 spec 落地**之后**的收尾:整体 review + 把 FFI/bridge 层做成"产品级",让别的项目低摩擦地继续开发。(1) 端到端 review —— 真零拷贝(共享后端可验证)、**uint 索引契约覆盖所有 molpy reader**、pair kernel 能量 parity、MMFF parity 后删 `frame_builder`、**无残留** `frame_marshal`/`from_lammps_ff`/MMFF 私有机器;一套 pairs 约定/一个 nblist 工具/一种 capsule 模式。(2) FFI 易用性 + **命名统一** —— `molrs-ffi::bridge` 一套 `verb_noun`、`py↔ref` 对称命名,capsule 名带版本;**一行采纳路径**(`use molrs_ffi::prelude::*` → `obj.as_frame_ref()?`/`frame_ref.into_py(py)?`,隐藏 addr/capsule 细节);单一 error 类型;扁平公开面(`molrs::io::forcefield::{read_lammps,read_opls,ForceFieldReader}`,无深路径泄漏)。(3) `docs/interop.md` 钦定配方 + ~10 行可跑示例 + 数据契约 + 命名约定,molpack 作参考。依赖 [interop-ffi-bridge](interop-ffi-bridge.md) + [ff-special-bonds-nblist](ff-special-bonds-nblist.md);命名决定要在外部项目 pin 公开面**之前**落 |
| 2026-06-13 | [ff-special-bonds-nblist](ff-special-bonds-nblist.md) | draft | molrs-ff (potential/forcefield/typifier/mmff), molrs-python (consumer: molpack relaxer) | 一条通用 potential 路径,统掉两套 pair 约定 + 一处错位职责。**neighbor list 不归 ForceField** —— 由消费方(优化器/积分器)建:`intramolecular_pairs(frame)→atomi/atomj/is_14`(已抽好,从 MMFF frame_builder 提出)。`ForceField` 只持 **special_bonds 权重**(amber lj 0/0/0.5、coul 0/0/0.8333,reader 填),`to_potentials(&frame)` 不变、只消费"已带 pairs 的 frame"。通用 `lj/cut`/`coul/cut` ctor 改 **逐原子组合(LB)+ is_14 缩放**(对齐 mmff_vdw,单一 `atomi/atomj/is_14` 块格式),改其单测。**MMFF 降级为纯参数载入**:删 `frame_builder` 私有建 Frame/pairs,`build()`=typify→ForceField,`mmff_*` kernel 经通用 `to_potentials` 消费(严禁另起炉灶)。参考消费方 molpack `LBFGSRelaxer::new(ff,frame)` 自建 nblist;`from_lammps_ff` 删,LAMMPS input 由 `molrs::io::forcefield` 读。与 [interop-ffi-bridge](interop-ffi-bridge.md) 组合:relaxer 经 bridge 吃 `molrs.ForceField` |
| 2026-06-13 | [interop-ffi-bridge](interop-ffi-bridge.md) | draft | molrs-ffi, molrs-python, molrs-capi (consumer: molpack) | 下游 Rust/PyO3 应用低摩擦复用 molpy/molrs 数据对象的**唯一钦定零拷贝路径**:废掉每个消费方各自的 marshal（molpack `frame_marshal` 的 `to_dict/from_dict` 往返 = 并行数据层 + 每次 O(N) 拷贝 + int32/UInt 错位 bug）。复用已存在的稳定 FFI:`Frame._ffi_frameref_capsule()`(`"molrs.FrameRef"` PyCapsule,共享句柄零拷贝)+ `molrs-cxxapi::frame_clone_from_addr`。补缺口:`molrs-ffi::bridge` 消费侧助手(frame/forcefield/block 的 capsule↔handle 双向)、给 `ForceField` 补 `_ffi_*_capsule()` + Frame 回程构造、capi 句柄补全、**uint 索引数据契约**、capsule ABI 版本守卫。消费方只链 `molrs-ffi`、不持自有类型、经句柄 API 操作;molpack 作参考迁移删 `frame_marshal`。ff potential 统一(special_bonds/逐原子 pair/MMFF 降级/`io::forcefield`)另立 spec,二者组合:bridge 落地后 relaxer 经 bridge 吃 `molrs.ForceField` |
| 2026-06-13 | [workspace-single-crate-merge](workspace-single-crate-merge.md) | code-complete | molrs (all crates) | 把 7 个成员 crate（core/io/signal/compute/ff/conformer + 门面）合并成单个 `molcrafts-molrs`，子系统改 feature-gated **模块**（源码物理吸收，~807 处引用→`crate::` 路径、166 文件）；binder（ffi/cxxapi/wasm/python）+ 外部 molpack **重指**到合并 crate + feature；公开 API 表面零变化；publish.yml 7 个 cargo-publish→1。**必须原子落地**（前向增量会构成依赖环）；子 crate 名不 yank（molpack 0.1.0 pin 仍解析），`molcrafts-molrs` 走 breaking 版本跳 |
| 2026-06-12 | [core-drop-petgraph](core-drop-petgraph.md) | approved | molrs-core | 从 molrs-core 移除 petgraph：`Topology`/`topo_distances`/`find_rings` 改在 MolGraph 原生邻接快照上跑（angles/dihedrals/impropers 枚举、BFS 距离、flood-fill 连通分量、Horton SSSR 全是手写，仅 3 处 `petgraph::algo::connected_components` 计数需换原生），删 `petgraph = "0.8"`；行为逐字节等价，topo_distances 不退化（PoC 1.4–1.5×）；取代 topology-paths-molgraph-01；SMARTS VF2（molrs-io）留作独立 vendor spec |
| 2026-06-12 | [gaff-typifier-01-parser](gaff-typifier-01-parser.md) | approved | molrs-ff | GAFF M1 chain 1/4：原生 gaff.dat(GAFF 1.x) reader → molrs ForceField/Params + 编译期内嵌（镜像 MMFF94_XML，留在 molrs-ff）；gaff.dat 从 openmmforcefields(MIT) vendored+版本钉死，参数对照其转换 ffxml 作 AmberTools-free ground truth；覆盖 MASS/BOND/ANGLE/DIHEDRAL/IMPROPER/NONBON；负周期多项二面角续行累加 + improper 无 IDIVF 为载重不变量 |
| 2026-06-12 | [gaff-typifier-02-typing](gaff-typifier-02-typing.md) | approved | molrs-ff | GAFF M1 chain 2/4：非共轭原子分型引擎（H/C/N/O/S/P/卤素局部判定），镜像 typifier/mmff，复用 rings/aromaticity/hybrid；ATOMTYPE_GFF.DEF 为基准；ca 入 nb 出的刻意非对称；共轭/杂芳输入必须报错不静默误型 |
| 2026-06-12 | [gaff-typifier-03-assign](gaff-typifier-03-assign.md) | approved | molrs-ff | GAFF M1 chain 3/4：键/角/二面角/improper 参数分配，X 通配回退（精确优先、最具体通配次之）独立于通用 store；多项二面角组装；improper PK 直用；产出已参数化 Frame→to_potentials |
| 2026-06-12 | [gaff-typifier-04-parity](gaff-typifier-04-parity.md) | approved | molrs-ff | GAFF M1 chain 4/4：antechamber 对照验证台（gen_gaff_fixtures.py 跑 antechamber -at gaff → SDF+JSON；逐原子 100% 一致 type:scientific 门），fixtures 缺席时干净跳过；镜像 MMFF/RDKit fixture 范式 |
| 2026-06-12 | [gaff-typifier-05-charges](gaff-typifier-05-charges.md) | approved | molpy-wrapper, molrs-ff | GAFF 电荷：与 openmm 一致 = AM1-BCC 委托（antechamber -c bcc / openff am1bcc），不在 molrs 重写半经验 QM；电荷在委托层算好写进 frame charge 列，molrs 只消费；逐原子对照 omff/antechamber（gated），净电荷守恒、禁 Gasteiger 替代；未来 QM-free 走 NAGL GNN |
| 2026-06-10 | [topology-paths-molgraph-01](topology-paths-molgraph-01.md) | superseded | molrs-core, molrs-python | 下沉 #1：角/二面角=键图里的 k-边路径 + `Atomistic::generate_topology`。**superseded by [core-drop-petgraph](core-drop-petgraph.md)** —— 当时按"用 petgraph 不要重造轮子"改成复用 petgraph Topology；core-drop-petgraph 据 PoC 反转该决策、原生化并删依赖 |
| 2026-06-10 | [ff-potentials-oop-01](ff-potentials-oop-01.md) | code-complete | molrs-ff, molrs-python, molrs-conformer | 下沉 #2：镜像 molpy OOP — `Style::to_potential`/`ForceField::to_potentials`(无 frame)/`Potentials::calc_energy\|calc_forces(frame)` + `LBFGS` 优化器类(`new(pot,cfg).run(frame)`)；删 compile/eval/自由 minimize/自由 ctor；ETKDG 用私有引擎不变 |
| 2026-06-10 | [ff-format-readers-01](ff-format-readers-01.md) | code-complete | molrs-ff, molrs-python | 下沉 #3：molrs 直接读力场格式 → molrs.ForceField（单位归一化在 reader）；首个 OplsXmlReader（GROMACS nm/kJ→Å/kcal，RB→OPLS f1..f4）；对照 molpy numpy OPLS |
| 2026-06-10 | [opls-ef-01-kernels-seam](opls-ef-01-kernels-seam.md) | code-complete | molrs-ff | OPLS-AA E/F：补 OPLS 4-cosine 二面角 + 通用 coul/cut 库仑 kernel（bond/angle 复用 harmonic），定义 molpy SMARTS-typify→typed Frame→molrs compile→minimize 接缝；排除/1-4 scaling/几何组合规则在 molpy 侧烘焙进 pairs 块，kernel 拓扑无关；对照 molpy numpy OPLS。typifier 不下沉（B线） |
| 2026-06-10 | [geometry-optimizer-01-generic-batch](geometry-optimizer-01-generic-batch.md) | code-complete | molrs-ff, molrs-conformer, molrs-python | 通用几何优化器下沉：把 ETKDG 私有 L-BFGS 抽进 molrs-ff 成 force-field-agnostic minimize（fmax 收敛，复用 two-loop+line search）+ 同构体系批量 minimize_batch（rayon par_chunks，(B,N,3)）+ PyO3 暴露 Potentials.minimize/minimize_batch；ETKDG 行为不变；对照 molpy numpy-LBFGS & RDKit MMFFOptimize |
| 2026-06-08 | [molgraph-ecs-01-core](molgraph-ecs-01-core.md) | draft | molrs-core | MolGraph→ECS：图实例即 world(纯数据,无全局/scheduler/手创)；system 即吃 world 的自由函数(perceive_aromaticity(mol)…)；component=共享行序对齐稠密列+null 掩码(零拷贝/对齐/O(1)/句柄稳定)；零硬编码字段=引用内置 key 约定+直接访问+错即报错。chain molgraph-ecs 1/3(inversion 量级,breaking molrs) |
| 2026-06-08 | [molgraph-ecs-02-pybind](molgraph-ecs-02-pybind.md) | draft | molrs-python | 暴露 ECS world 到 Python：entity 稳定句柄(int)+component 列零拷贝 numpy view(写穿)+system 模块自由函数(molrs.perceive_aromaticity(mol))+叶子可子类化+零拷贝 adopt(chain molgraph-ecs 2/3,依赖 01) |
| 2026-06-08 | [molgraph-abstract-01-core](molgraph-abstract-01-core.md) | code-complete | molrs-core | 反转 MolGraph 为领域无关 interned kind 标签 n 元关系存储（SmallVec nodes/Option props/KindId 数组索引）；领域词下沉 Atomistic/CoarseGrain 叶子；dihedral/improper→4 元+kind；修 merge 漏 impropers bug；预留 containment 轴（chain molgraph-abstract 1/2，refines molgraph-pybind-01 D2） |
| 2026-06-08 | [molgraph-abstract-02-pybind](molgraph-abstract-02-pybind.md) | approved | molrs-python | Python 暴露反转层级：PyGraph 仅通用关系 API + 无字段 add_node，领域方法移到 PyAtomistic/PyCoarseGrain 叶子；向后兼容 molpy to_molrs/embed 不回归（chain molgraph-abstract 2/2，依赖 01） |
| 2026-06-01 | [molgraph-pybind-01-hierarchy](molgraph-pybind-01-hierarchy.md) | code-complete | molrs-core, molrs-python | 暴露 MolGraph 层级到 Python（Graph ← Atomistic/CoarseGrain）+ 角/二面角/impropers/属性/extend 绑定扩面；molpy 子类化后端的先决项 P0（5/5 acceptance verified） |
| 2026-06-01 | [core-perception-01-aromaticity](core-perception-01-aromaticity.md) | approved | molrs-core | Atomistic::perceive_aromaticity()，对齐 RDKit 默认芳香性模型（chain core-perception 1/2） |
| 2026-06-01 | [core-perception-02-smarts-rings](core-perception-02-smarts-rings.md) | approved | molrs-core | SMARTS r{lo-hi} 区间 + x<n> 环连接度，去掉 torsion_prefs shim（chain core-perception 2/2） |
| 2026-06-01 | [etkdg-smarts-01-engine](etkdg-smarts-01-engine.md) | code-complete | molrs-core | SMARTS 子结构匹配引擎（含递归 $()），对照 RDKit GetSubstructMatches（chain etkdg-smarts 1/2） |
| 2026-06-01 | [etkdg-smarts-02-torsions](etkdg-smarts-02-torsions.md) | code-complete | molrs-embed | ETKDGv3 完整实验扭转表 + SMARTS 接入，闭合 mmff94-etkdg-04 RMSD（chain etkdg-smarts 2/2） |
| 2026-06-01 | [mmff94-etkdg-01-typing](mmff94-etkdg-01-typing.md) | code-complete | molrs-ff | MMFF94 原子分型 + 芳香性 + 参数表 + BCI 电荷（对照 RDKit，chain mmff94-etkdg 1/4） |
| 2026-06-01 | [mmff94-etkdg-02-energy](mmff94-etkdg-02-energy.md) | code-complete | molrs-ff | MMFF94/MMFF94s 七项能量 + 解析梯度（对照 RDKit，chain mmff94-etkdg 2/4） |
| 2026-06-01 | [mmff94-etkdg-03-bounds](mmff94-etkdg-03-bounds.md) | code-complete | molrs-embed | DistGeom bounds 矩阵（对齐 RDKit 0.0）+ 三角 smoothing + 手性；实验扭转部分（待 etkdg-smarts）（chain mmff94-etkdg 3/4） |
| 2026-06-01 | [mmff94-etkdg-04-embed](mmff94-etkdg-04-embed.md) | code-complete | molrs-embed | 4D 嵌入 + ET 最小化 + MMFF 清理 + 重试,重写 generate_3d；删除/reshape 推迟（chain mmff94-etkdg 4/4） |
| 2026-05-17 | [vibrational-spectra](vibrational-spectra.md) | approved | molrs-compute, molrs-signal | 功率谱 (VDOS)、红外、拉曼光谱 — ndarray 输入，复用 ACF/窗函数/频率网格 |
| Date | Slug | Status | Owner crate(s) | Summary |
|---|---|---|---|---|
| 2026-06-10 | [unit-conversion-system](unit-conversion-system.md) | shipped | molrs-core | pint-style UnitRegistry + Dimension + Quantity; MD units (kcal/mol, eV, hartree, Å, bohr, °C) |

<!--
Status values:
  draft      — spec written, not yet implemented
  in-flight  — /mol:impl started against this spec
  shipped    — merged to master
  superseded — replaced by a later spec (link it in Summary)
-->
